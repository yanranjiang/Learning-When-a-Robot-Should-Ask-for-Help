# Anonymous paper project page

This is a dependency-free static site. Preview it locally from this directory:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

For anonymous peer review, publish this directory through the anonymized repository service. The response letter and editable manuscript sources are intentionally excluded because they contain editorial correspondence, internal notes, and identifying filenames.
